// ─────────────────────────────────────────────────────────────
// Quartermaster — Optional composition module entry point.
// ─────────────────────────────────────────────────────────────

export type {
  ComposableArtifact,
  CompositionChain,
  CompositionEdge,
  CompositionIssue,
  CompositionRole,
  CompositionValidationResult,
} from './model';
export { validateComposition } from './validate';
