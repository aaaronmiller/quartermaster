# MODEL START HERE

This folder is a living document.

Read in this order:

1. `RAISON_DETRE.md`
2. `public/content/index.json`
3. targeted files in `public/content/sections/`
4. `public/data/annotations.json`
5. `resources/`

Do not modify `public/index.html`, `public/styles.css`, or `public/app.js` unless the user explicitly asks to change the interface. Put content in Markdown sections, update manifest records, append history/worklog entries, and validate.

Use `modelReplies` in the manifest for questions, answer options, or clarifications that the human can handle asynchronously. Continue unblocked work instead of stopping the whole project for one open reply.
